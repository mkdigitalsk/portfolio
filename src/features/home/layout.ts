// Shared layout widths for the home sections — the page's horizontal rhythm lives here, in
// one place, instead of hardcoded per section. Tune the whole page from here.
export const SECTION_MAX = 960 // standard section content width
export const SECTION_MAX_WIDE = 1180 // wider — multi-column grids (services, process, trust band)
export const TEXT_MAX = 680 // readable text column (~65–75 characters per line)
